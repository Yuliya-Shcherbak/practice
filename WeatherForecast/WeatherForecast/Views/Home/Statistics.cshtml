@model List<WeatherForecast.Models.City>

@{
    ViewBag.Title = "Statistics";
    Layout = "~/Views/Shared/_Layout.cshtml";
}

<h2>Statistics</h2>

@foreach (var m in Model)
{
    <div> @Html.ActionLink(m.name, "ShowStatistics", "Home", new { id = m.id }, null)</div>
}

